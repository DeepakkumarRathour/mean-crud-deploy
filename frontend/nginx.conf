server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve Angular SPA — fallback to index.html for routes
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optionally, you can add caching / gzip / headers here
}
